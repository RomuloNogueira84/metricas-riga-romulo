import { start } from './server.js';

start();

//O arquivo index.js é o ponto de entrada do nosso servidor. Ele é responsável por importar a função start() do server.js e iniciar o servidor, garantindo que ele fique disponível para receber requisições.
